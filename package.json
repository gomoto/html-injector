{
  "name": "html-injector",
  "version": "1.1.1",
  "description": "Inject file paths and file content into an HTML file",
  "main": "src/index.js",
  "bin": {
    "html-injector": "./cli.js"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/gomoto/html-injector.git"
  },
  "license": "MIT",
  "dependencies": {
    "commander": "^2.9.0",
    "glob": "^7.0.3",
    "replacestream": "^4.0.0"
  }
}
